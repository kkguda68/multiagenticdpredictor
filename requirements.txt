# ---------------------------------------------------------------------------
# ICD-10 Multi-Agent Orchestration System - Python Dependencies
# Target runtime: Python 3.11+  (Google Cloud Run / Vertex AI Workbench)
# ---------------------------------------------------------------------------

# --- Core Orchestration (Google Agent Development Kit) ---
google-adk>=1.2.0

# --- Google Cloud Platform SDKs ---
google-cloud-dlp>=3.24.0               # Data Loss Prevention (PHI scrubbing)
google-genai>=0.3.0                    # Gemini SDK
google-cloud-bigquery>=3.25.0          # BigQuery client
google-cloud-bigquery-storage>=2.27.0  # Faster BQ result streaming
db-dtypes>=1.3.0                       # BigQuery -> pandas type support

# --- Agent 2: local PubMedBERT (Hugging Face) ---
transformers>=4.45.0
torch>=2.4.0
numpy>=1.26.0

# --- Data Validation & Settings ---
pydantic>=2.9.0
pydantic-settings>=2.5.0

# --- Async / Utility ---
tenacity>=9.0.0                        # Retry with exponential backoff
python-dotenv>=1.0.1
structlog>=24.4.0                      # Structured JSON logging

# --- Optional API surface (if deploying as a service) ---
fastapi>=0.115.0
uvicorn[standard]>=0.31.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
